# Variáveis de ambiente do Ablaw IA.
# Nenhum valor real deve ser commitado. O projeto Supabase real ainda não
# existe (será criado em fase futura) — estas chaves são apenas a forma
# documentada de como o app vai se conectar a ele quando existir.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
